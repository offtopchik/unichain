#!/bin/bash

# Создаем скрипт unichain.sh
cat << 'EOF' > unichain.sh
#!/bin/bash

# Цвета текста
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[0;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
NC='\033[0m' # Сброс цвета

# Функция для установки ноды
function install_node() {
    echo -e "${CYAN}>> Устанавливаем систему и зависимости...${NC}"
    sudo apt update && sudo apt upgrade -y
    sudo apt install -y git curl build-essential python3 python3-pip

    echo -e "${CYAN}>> Устанавливаем Node.js...${NC}"
    curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
    sudo apt install -y nodejs

    echo -e "${CYAN}>> Устанавливаем Docker...${NC}"
    sudo apt install -y apt-transport-https ca-certificates software-properties-common
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
    sudo apt update
    sudo apt install -y docker-ce docker-ce-cli containerd.io

    echo -e "${CYAN}>> Устанавливаем Docker Compose...${NC}"
    sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
    sudo chmod +x /usr/local/bin/docker-compose

    echo -e "${CYAN}>> Клонируем репозиторий Unichain Node...${NC}"
    git clone https://github.com/Uniswap/unichain-node.git || echo -e "${YELLOW}Репозиторий уже клонирован.${NC}"
    cd unichain-node || { echo -e "${RED}Ошибка: каталог unichain-node не найден!${NC}"; exit 1; }

    echo -e "${CYAN}>> Проверяем наличие package.json...${NC}"
    if [ ! -f "package.json" ]; then
        echo -e "${RED}Ошибка: файл package.json отсутствует. Проверьте репозиторий.${NC}"
        exit 1
    fi

    echo -e "${CYAN}>> Устанавливаем зависимости npm...${NC}"
    npm install

    echo -e "${CYAN}>> Запускаем ноду...${NC}"
    npm run start &
    echo -e "${GREEN}>> Установка завершена. Нода запущена.${NC}"
}

# Функции управления нодой
function restart_node() {
    echo -e "${YELLOW}>> Перезапускаем ноду...${NC}"
    cd unichain-node || { echo -e "${RED}Каталог unichain-node не найден!${NC}"; return; }
    npm run stop || echo -e "${RED}Нода не была запущена.${NC}"
    npm run start &
    echo -e "${GREEN}>> Нода успешно перезапущена.${NC}"
}

function stop_node() {
    echo -e "${RED}>> Останавливаем ноду...${NC}"
    cd unichain-node || { echo -e "${RED}Каталог unichain-node не найден!${NC}"; return; }
    npm run stop
    echo -e "${GREEN}>> Нода успешно остановлена.${NC}"
}

function check_node() {
    echo -e "${BLUE}>> Проверяем состояние ноды...${NC}"
    docker ps
}

function view_logs_op() {
    echo -e "${CYAN}>> Логи Unichain OP (нажмите Ctrl+C для выхода)...${NC}"
    docker logs --follow unichain-op
}

function view_logs_unichain() {
    echo -e "${CYAN}>> Логи Unichain (нажмите Ctrl+C для выхода)...${NC}"
    docker logs --follow unichain
}

function view_private_key() {
    echo -e "${BLUE}>> Просмотр приватного ключа...${NC}"
    cd unichain-node || { echo -e "${RED}Каталог unichain-node не найден!${NC}"; return; }
    if [ -f nodekey ]; then
        echo -e "${GREEN}Приватный ключ:${NC}"
        cat nodekey
    else
        echo -e "${RED}Файл nodekey не найден. Приватный ключ отсутствует.${NC}"
    fi
}

function change_private_key() {
    echo -e "${YELLOW}>> Изменение приватного ключа...${NC}"
    cd unichain-node || { echo -e "${RED}Каталог unichain-node не найден!${NC}"; return; }
    if [ ! -f nodekey ]; then
        echo -e "${YELLOW}Файл nodekey не найден. Создаем новый...${NC}"
        touch nodekey
    fi
    read -p "Введите новый приватный ключ: " new_key
    echo "$new_key" > nodekey
    echo -e "${GREEN}>> Приватный ключ успешно обновлен.${NC}"
}

# Красивое меню
while true; do
    clear
    echo -e "${CYAN}============================================${NC}"
    echo -e "${CYAN}       🌟 Меню управления Unichain Node 🌟${NC}"
    echo -e "${CYAN}============================================${NC}"
    echo -e "${GREEN}1. Установить ноду${NC}"
    echo -e "${GREEN}2. Перезапустить ноду${NC}"
    echo -e "${GREEN}3. Проверить состояние ноды${NC}"
    echo -e "${GREEN}4. Остановить ноду${NC}"
    echo -e "${GREEN}5. Просмотр логов Unichain OP${NC}"
    echo -e "${GREEN}6. Просмотр логов Unichain${NC}"
    echo -e "${GREEN}7. Просмотреть приватный ключ${NC}"
    echo -e "${GREEN}8. Изменить приватный ключ${NC}"
    echo -e "${RED}9. Выйти${NC}"
    echo -e "${CYAN}============================================${NC}"
    read -p "Выберите действие (1-9): " choice

    case $choice in
        1) install_node ;;
        2) restart_node ;;
        3) check_node ;;
        4) stop_node ;;
        5) view_logs_op ;;
        6) view_logs_unichain ;;
        7) view_private_key ;;
        8) change_private_key ;;
        9) echo -e "${RED}Выход из скрипта.${NC}"; break ;;
        *) echo -e "${RED}Неверный выбор. Пожалуйста, выберите от 1 до 9.${NC}" ;;
    esac
    read -p "Нажмите Enter для возврата в меню..."
done
EOF

# Делаем созданный скрипт исполняемым
chmod +x unichain.sh

# Сообщение об успешном создании и автозапуск меню
echo -e "${GREEN}Скрипт 'unichain.sh' успешно создан!${NC}"
echo -e "Запускаем меню управления..."
bash unichain.sh
