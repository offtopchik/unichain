#!/bin/bash

# Цвета для вывода
RED="\033[0;31m"
GREEN="\033[0;32m"
BLUE="\033[0;34m"
YELLOW="\033[1;33m"
CYAN="\033[0;36m"
RESET="\033[0m"

# Функция отображения логотипа
channel_logo() {
  echo -e "${CYAN}"
  echo "========================================"
  echo "🚀 Welcome to the Unichain Node Manager 🚀"
  echo "========================================"
  echo -e "${RESET}"
}

# Функции
download_node() {
  echo -e "${GREEN}Начинаю установку...${RESET}"

  sudo apt update -y && sudo apt upgrade -y
  sudo apt-get install make build-essential unzip lz4 gcc git jq -y

  sudo apt install docker.io -y

  sudo systemctl start docker
  sudo systemctl enable docker

  sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
  sudo chmod +x /usr/local/bin/docker-compose

  git clone https://github.com/Uniswap/unichain-node
  cd unichain-node || { echo -e "${RED}Не получилось зайти в директорию${RESET}"; return; }
  sudo docker-compose up -d

  echo -e "${GREEN}Установка завершена! Нода запущена.${RESET}"
}

restart_node() {
  HOMEDIR="$HOME"
  echo -e "${YELLOW}Перезагружаю ноду...${RESET}"
  sudo docker-compose -f "${HOMEDIR}/unichain-node/docker-compose.yml" down
  sudo docker-compose -f "${HOMEDIR}/unichain-node/docker-compose.yml" up -d
  echo -e "${GREEN}Unichain был перезагружен.${RESET}"
}

check_node() {
  echo -e "${BLUE}Проверяю ноду...${RESET}"
  response=$(curl -s -d '{"id":1,"jsonrpc":"2.0","method":"eth_getBlockByNumber","params":["latest",false]}' \
    -H "Content-Type: application/json" http://localhost:8545)

  if [[ -n "$response" ]]; then
    echo -e "${GREEN}RESPONSE:${RESET} $response"
  else
    echo -e "${RED}Не удалось подключиться к ноде. Проверьте настройки.${RESET}"
  fi
}

check_logs_op_node() {
  echo -e "${CYAN}Логи Unichain (OP):${RESET}"
  sudo docker logs unichain-node-op-node-1
}

check_logs_unichain() {
  echo -e "${CYAN}Логи Unichain:${RESET}"
  sudo docker logs unichain-node-execution-client-1
}

stop_node() {
  HOMEDIR="$HOME"
  echo -e "${YELLOW}Останавливаю ноду...${RESET}"
  sudo docker-compose -f "${HOMEDIR}/unichain-node/docker-compose.yml" down
  echo -e "${GREEN}Нода остановлена.${RESET}"
}

display_private_key() {
  echo -e "${YELLOW}Ваш приватный ключ:${RESET}"
  cat "$HOME/unichain-node/geth-data/geth/nodekey" 2>/dev/null || echo -e "${RED}Приватный ключ не найден.${RESET}"
}

change_private_key() {
  KEY_PATH="$HOME/unichain-node/geth-data/geth/nodekey"
  if [ ! -f "$KEY_PATH" ]; then
    echo -e "${RED}Файл приватного ключа не найден. Проверьте путь и наличие ноды.${RESET}"
    return
  fi

  read -p "Введите новый приватный ключ: " new_key
  if [[ -z "$new_key" ]]; then
    echo -e "${RED}Приватный ключ не может быть пустым.${RESET}"
    return
  fi

  echo "$new_key" >"$KEY_PATH"
  echo -e "${GREEN}Приватный ключ успешно обновлен.${RESET}"

  # Перезапуск ноды для применения изменений
  echo -e "${YELLOW}Перезапускаю ноду для применения нового ключа...${RESET}"
  stop_node
  restart_node
}

exit_from_script() {
  echo -e "${CYAN}Спасибо за использование! До свидания!${RESET}"
  exit 0
}

# Главное меню
while true; do
  clear
  channel_logo
  echo -e "\n${YELLOW}Меню:${RESET}"
  echo -e "${GREEN}1. 🚀 Установить ноду${RESET}"
  echo -e "${GREEN}2. 🔄 Перезагрузить ноду${RESET}"
  echo -e "${GREEN}3. ✅ Проверить ноду${RESET}"
  echo -e "${GREEN}4. 📜 Посмотреть логи Unichain (OP)${RESET}"
  echo -e "${GREEN}5. 📜 Посмотреть логи Unichain${RESET}"
  echo -e "${GREEN}6. 🛑 Остановить ноду${RESET}"
  echo -e "${GREEN}7. 🔑 Посмотреть приватный ключ${RESET}"
  echo -e "${GREEN}8. 🔄 Изменить приватный ключ${RESET}"
  echo -e "${RED}9. ❌ Выйти из скрипта${RESET}\n"

  read -p "Выберите пункт меню: " choice

  case $choice in
  1)
    download_node
    ;;
  2)
    restart_node
    ;;
  3)
    check_node
    ;;
  4)
    check_logs_op_node
    ;;
  5)
    check_logs_unichain
    ;;
  6)
    stop_node
    ;;
  7)
    display_private_key
    ;;
  8)
    change_private_key
    ;;
  9)
    exit_from_script
    ;;
  *)
    echo -e "${RED}Неверный пункт. Пожалуйста, выберите цифру из меню.${RESET}"
    sleep 2
    ;;
  esac

  echo -e "\n${BLUE}Нажмите Enter, чтобы вернуться в меню...${RESET}"
  read
done
